# General configuration
# Optional: if you prefer to keep non-sensitive values here
BIGQUERY_PROJECT_ID=
BIGQUERY_DATASET=
BIGQUERY_TABLE=

# For local dev, put secrets in .secrets/.env (gitignored).
# Example for WSL:
# GOOGLE_APPLICATION_CREDENTIALS=/home/your_user/repositories/web-mapa-do-bosque/wbr-streamlit-bigquery/.secrets/service-account.json
